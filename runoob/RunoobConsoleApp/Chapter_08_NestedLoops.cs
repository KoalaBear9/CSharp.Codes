

using System.Net;

/**
 * 菜鸟教程控制台应用示例
 * 
 * @author  Jason
 * @date    2025-02-11
 */
namespace RunoobConsoleApp
{

    /// <summary>
    /// Chapter_01_Basic_Syntax 类包含了演示C#基本语法的示例代码
    /// </summary>
    internal class Chapter_08_NestedLoops
    {
        /// 程序的主入口点
        /// </summary>
        /// <param name="args">命令行参数</param>
        /*
        public static void Main(string[] args)
        {
            {
                // 局部变量定义
                int i, j;

                for (i = 2; i < 100; i++)
                {
                    for (j = 2; j <= (i / j); j++)
                    {
                        if ((i % j) == 0)
                        {
                            break;
                        } // 如果找到，则不是质数
                    }
                    if (j > (i / j))
                        Console.WriteLine("{0} 是质数", i);
                }
            }
            Console.ReadLine();
        }
        */
    }
}