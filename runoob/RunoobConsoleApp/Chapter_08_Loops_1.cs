

using System.Net;

/**
 * 菜鸟教程控制台应用示例
 * 
 * @author  Jason
 * @date    2025-02-11
 */
namespace RunoobConsoleApp
{

    /// <summary>
    /// Chapter_01_Basic_Syntax 类包含了演示C#基本语法的示例代码
    /// </summary>
    internal class Chapter_08_Loops_1
    {
        /// 程序的主入口点
        /// </summary>
        /// <param name="args">命令行参数</param>
        /* 
        public static void Main(string[] args)
        {
            {
                int n = 50;
                decimal[] feiBos = new decimal[n];
                feiBos[0] = 1; // 数组从0开始
                feiBos[1] = 1; // 定义了前两项
                // while 循环，计算数组斐波那契数列的每一项
                int x = 2;
                while (x <= n - 1)
                {
                    feiBos[x] = feiBos[x - 1] + feiBos[x - 2];
                    x++;
                }

                foreach (decimal i in feiBos)
                {
                    Console.WriteLine(i);
                }
            }

            {
                // 打印出斐波那契数列的前50项
                double[] feiBos = new double[50];
                feiBos[0] = 1; // 数组从0开始
                feiBos[1] = 1; // 定义了前两项

                for (int x = 2; x < 50; x++)
                {
                    feiBos[x] = feiBos[x - 1] + feiBos[x - 2];
                }

                Console.WriteLine("feiBos = [" + string.Join(",", feiBos) + "]");
                Console.WriteLine("feiBo数组的长度是{0}", feiBos.Length);
            }

            Console.ReadLine();
        }
        */
    }
}