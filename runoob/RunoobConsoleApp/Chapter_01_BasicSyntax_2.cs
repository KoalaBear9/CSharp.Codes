// 顶级语句的 C# 代码

/*
using System;
using System.Linq;

Console.WriteLine("你好，世界!");

 Console.WriteLine("A:{0}，a:{1}",65,97);

// 顶级语句中的变量声明
int number = 42;
string message = "生命、宇宙和一切的答案是";

// 输出变量
Console.WriteLine($"{message} {number}.");

// 定义和调用方法
int Add(int a, int b) => a + b;
Console.WriteLine($"1和2之和为: {Add(1, 2)}.");

var numbers = new[] { 1, 2, 3, 4, 5 };
var evens = numbers.Where(n => n % 2 == 0).ToArray();
Console.WriteLine($"偶数: {string.Join(", ", evens)}.");

// 异常处理
try
{
    int zero = 0;
    int result = number / zero;
}
catch (DivideByZeroException ex)
{
    Console.WriteLine("输入的不是一个数字!");
    Console.WriteLine($"错误信息1：{ex.Message}");
    Console.WriteLine("错误信息2: " + ex.Message);
    Console.WriteLine("错误信息3: {0}", ex.Message);
    Console.WriteLine("堆栈跟踪: " + ex.StackTrace);
}
*/