
/**
 * 菜鸟教程控制台应用示例
 * 
 * @author  Jason
 * @date    2025-02-11
 */
namespace RunoobConsoleApp
{

    /// <summary>
    /// Chapter_01_Basic_Syntax 类包含了演示C#基本语法的示例代码
    /// </summary>
    internal class Chapter_06_Operators_1
    {
        /// 程序的主入口点
        /// </summary>
        /// <param name="args">命令行参数</param>
        /*
        public static void Main(string[] args)
        {
            {
                int a = 21;
                int b = 10;
                int c;

                c = a + b;
                Console.WriteLine("a+b=" + c);

                c = a - b;
                Console.WriteLine("a-b=" + c);

                c = a * b;
                Console.WriteLine("a*b=" + c);

                c = a / b;
                Console.WriteLine("a/b=" + c);

                c = a % b;
                Console.WriteLine("a%b=" + c);

                // ++a 先进行自增运算再赋值
                c = ++a;
                Console.WriteLine("++a=" + c);

                // 此时 a 的值为 22
                // --a 先进行自减运算再赋值
                c = --a;
                Console.WriteLine("--a=" + c);
                // Console.ReadLine();
            }

            {
                int a = 1;
                int b;

                // a++ 先赋值再进行自增运算
                b = a++;
                Console.WriteLine("a = {0}", a);
                Console.WriteLine("b = {0}", b);
                Console.ReadLine();

                // ++a 先进行自增运算再赋值
                a = 1; // 重新初始化 a
                b = ++a;
                Console.WriteLine("a = {0}", a);
                Console.WriteLine("b = {0}", b);
                Console.ReadLine();

                // a-- 先赋值再进行自减运算
                a = 1;  // 重新初始化 a
                b = a--;
                Console.WriteLine("a = {0}", a);
                Console.WriteLine("b = {0}", b);
                Console.ReadLine();

                // --a 先进行自减运算再赋值
                a = 1;  // 重新初始化 a
                b = --a;
                Console.WriteLine("a = {0}", a);
                Console.WriteLine("b = {0}", b);
                Console.ReadLine();
            }
        }
        */
    }
}